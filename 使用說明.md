# 使用說明(給非工程師)

這是一個**每天/每週自動幫你抓「熱門排行榜」的資料庫**,你不用開電腦、不用管它。

## 它會做什麼
- **每天台北時間早上 9:00**:抓 GitHub「當日新增星 Top 10」+ Hugging Face 熱門「模型/資料集/Spaces 各 Top 5」。
- **每週一台北時間早上 9:00**:額外抓 GitHub「本週新增星 Top 10」。
- 每次都會:①更新首頁 `README.md`(打開就是最新)②把當天結果追加到當月檔 `archive/YYYY-MM.md` ③存一份 CSV 到 `data/` ④開一則 Issue 並 @ 你 → **你信箱會收到 email 摘要**。

## 我要去哪看
- **最新一天** → 打開 repo 首頁(`README.md`)。
- **email** → 每天早上收信,標題「📈 每日趨勢 日期」。
- **翻歷史** → `archive/`(每月一檔)、`weekly/`(每週)、`data/`(CSV,可丟 Excel 做圖表)。

## 我想改東西
- **改時間**:編輯 `.github/workflows/daily.yml` 裡的 `cron`(用 UTC;台北 = UTC+8)。
- **改名額**(例如 GitHub 改成 Top 20):編輯 `scripts/fetch_trends.py` 最上面的 `GH_TOP` / `HF_TOP`。
- **暫停**:到 repo 的 Actions 頁 → 選 workflow → 右上 `⋯` → Disable。想恢復再 Enable。
- **不想收 email**:到 repo 右上 `Watch` 改成 Ignore,或 GitHub 通知設定關掉;資料還是照常更新。

## 手動立刻跑一次
repo → Actions 頁 → 選 `daily-trend` 或 `weekly-trend` → 右邊 `Run workflow`。

## 已知限制
- GitHub Trending 沒有官方 API,靠解析官方頁面。GitHub 若大改版面,那天可能抓不到 → 那次 Action 會顯示失敗(紅色),你會在 Actions 頁看到;HF 部分不受影響。修法:更新 `scripts/fetch_trends.py` 裡的解析選擇器。
