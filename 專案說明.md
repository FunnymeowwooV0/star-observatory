# 觀星台(原趨勢雷達)— 專案說明(架構與決策)

每天/每週自動整理 GitHub 與 Hugging Face 熱門榜,存成可累積的資料庫、產出網頁,並由 AI 每週寫白話導讀。給非工程師使用。

## 三層架構

| 層 | 跑在哪 | 頻率 | 花 token? | 產出 |
|---|---|---|---|---|
| ① 抓原始榜 + 產網頁 | GitHub Actions(雲端,免費) | 每天 09:00 / 每週一 09:00(台北) | ❌ 零 | `README.md`、`docs/index.html`、`archive/`、`weekly/`、`data/*.csv`、Issue(=email) |
| ② AI 白話導讀 | 本機 Claude 排程任務 | 每週一 09:31(台北) | ✅ 少量 | 寫進 Obsidian `2-參考資料/觀星台/導讀_YYYY-Www.md` |
| ③ 進 Obsidian 副本 | 本機 `sync.sh`(手動/按需) | 手動 | ❌ 零 | 把 repo 的 .md 複製進 vault |

## 資料來源
- **GitHub**:`https://github.com/trending?since=daily|weekly`(官方頁,按「期間新增星」排序)。**無官方 API,靠解析 HTML** → 這是最大脆弱點,GitHub 改版面時當天會抓不到(Action 顯示失敗)。解析器有單元測試(`tests/`)守著,壞了會紅燈。
- **Hugging Face**:`https://huggingface.co/api/trending?type=model|dataset|space&limit=N`(官方 API,type 用單數,回傳 `{"recentlyTrending":[{"repoData":{...}}]}`)。

## 檔案地圖
- `scripts/fetch_trends.py` — 主程式。純解析函式 `parse_github_trending()`/`parse_hf_trending()`(可離線測)+ 連網 wrapper + Markdown/CSV 產出 + **網頁渲染 `render_html()` 與 `SITE_CSS`**。
- `tests/` — 解析器單元測試(離線 fixture)。`python -m unittest discover -s tests`。
- `.github/workflows/daily.yml` / `weekly.yml` — 抓榜排程。`tests.yml` — push 時跑測試。
- `docs/index.html` — 每日自動產的網頁 dashboard。
- `使用說明.md` — 給非工程師的操作說明。

## 常見修改
- **改網頁版面/配色** → `scripts/fetch_trends.py` 的 `render_html()` 與 `SITE_CSS` 兩塊,改完 `python scripts/fetch_trends.py --mode daily` 再開 `docs/index.html` 看。
- **改名額**(Top N) → `fetch_trends.py` 最上面 `GH_TOP` / `HF_TOP`。
- **改時間** → workflow 的 `cron`(UTC;台北 = UTC+8)。
- **改 AI 導讀內容/頻率** → 排程任務 `trend-radar-digest`(見本機 CLAUDE.md)。

## 關鍵決策(為什麼這樣做)
1. **原始榜用免費 GitHub Actions 而非叫 AI 每天跑** → 控制常駐成本,腳本抓資料不需要 AI。
2. **AI 導讀只做每週一次** → 白話+發想需要 LLM,但每天跑會燒 token;每週一次(跟週報同步)最省。
3. **不用 launchd 背景同步進 Obsidian** → macOS TCC 保護,launchd 叫起的程序碰不到 iCloud vault(`Operation not permitted`)。改由「有 vault 權限的本機 Claude 排程任務」寫入 + 手動 `sync.sh`。
4. **網頁固定網址用 GitHub Pages** → 免費方案需 repo 公開(資料本來就是公開熱門榜);待決。

## 待辦/待批准
- 🔔 是否把 repo 設公開以啟用免費 GitHub Pages 固定網址(公開前先抹除 commit 紀錄中的 gmail)。
- 網頁版面待使用者調整(改 `render_html`/`SITE_CSS`)。
