# 觀星台 — 專案說明(架構與決策)

每天/每週自動整理 GitHub 與 Hugging Face 等熱門榜,存成可累積的資料庫並產出網頁。給非工程師使用。

## 架構

| 層 | 跑在哪 | 頻率 | 產出 |
|---|---|---|---|
| 抓原始榜 + 產網頁 | GitHub Actions(雲端,免費) | 每天 09:00 / 每週一 09:00(台北) | `README.md`、`docs/index.html`、`archive/`、`weekly/`、`data/*.csv`、Issue(= email 摘要) |

抓資料與產網頁完全由排程的 GitHub Actions 完成,不需要開電腦、不需要 AI,零常駐成本。網頁是純靜態 HTML(inline CSS/JS),透過 GitHub Pages 提供固定網址。

## 資料來源
- **GitHub**:`https://github.com/trending?since=daily|weekly`(官方頁,按「期間新增星」排序)。**無官方 API,靠解析 HTML** → 這是最大脆弱點,GitHub 改版面時當天會抓不到(Action 顯示失敗)。解析器有單元測試(`tests/`)守著,壞了會紅燈。
- **Hugging Face**:`https://huggingface.co/api/trending?type=model|dataset|space&limit=N`(官方 API,type 用單數,回傳 `{"recentlyTrending":[{"repoData":{...}}]}`)。
- 另含 Hacker News、OpenRouter、Product Hunt、Ollama、Reddit 等來源(見 `scripts/sources_extra.py`)。

## 檔案地圖
- `scripts/fetch_trends.py` — 主程式。純解析函式 `parse_github_trending()`/`parse_hf_trending()`(可離線測)+ 連網 wrapper + Markdown/CSV 產出 + **網頁渲染 `render_html()` 與 `SITE_CSS`**。
- `scripts/sources_extra.py` — 額外資料來源解析器。
- `tests/` — 解析器與渲染契約單元測試(離線 fixture)。`python -m unittest discover -s tests`。
- `.github/workflows/daily.yml` / `weekly.yml` — 抓榜排程。`tests.yml` — push 時跑測試。`backfill-ph.yml` — 手動回補。
- `docs/index.html` — 每日自動產的網頁 dashboard。
- `使用說明.md` — 給非工程師的操作說明。

## 常見修改
- **改網頁版面/配色** → `scripts/fetch_trends.py` 的 `render_html()` 與 `SITE_CSS` 兩塊,改完 `python scripts/fetch_trends.py --mode daily` 再開 `docs/index.html` 看。
- **改名額**(Top N) → `fetch_trends.py` 最上面 `GH_TOP` / `HF_TOP`。
- **改時間** → workflow 的 `cron`(UTC;台北 = UTC+8)。

## 關鍵決策(為什麼這樣做)
1. **原始榜用免費 GitHub Actions 而非叫 AI 每天跑** → 控制成本,腳本抓資料不需要 AI。
2. **網頁做成純靜態、自包含** → 可直接放 GitHub Pages,沒有後端、沒有追蹤、沒有第三方相依(字型除外)。
3. **網頁固定網址用 GitHub Pages** → 免費;資料本來就是公開熱門榜。

## 資安
- CI 供應鏈:第三方 action 釘 commit SHA、pip 相依鎖版本 + 雜湊(`requirements.lock`,`--require-hashes`)、workflow 權限最小化(預設 `permissions: {}`)。
- 外部連結:所有來源 URL 經 `safe_external_url()` 收斂,只放行 https,阻擋 `javascript:`/`data:` 等,避免上游污染造成的 XSS。
- CSV:對外部字串做試算表公式注入中和。
